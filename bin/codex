#!/usr/bin/env bun
// vim: syntax=javascript

import * as fs from 'node:fs/promises';
import * as path from 'node:path';


const language = process.argv[2] ?? '';

const codexLocation = `/home/tony/.config/nvim/codex/${language}/`;

fs.readdir(codexLocation).
  then(console.log);



